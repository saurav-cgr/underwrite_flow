services:
  api:
    command:
      - sh
      - -c
      - "test \"$$GENERATION_PROVIDER\" = fake && python -m underwriteflow.auth.seed"
    environment:
      GENERATION_PROVIDER: fake
      LANGSMITH_TRACING: "false"
